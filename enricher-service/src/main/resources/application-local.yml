spring:
  kafka:
    bootstrap-servers: localhost:9092
  datasource:
    url: jdbc:postgresql://localhost:5432/jobsignal
    username: jobsignal
    password: localdev
  data:
    redis:
      host: localhost
      port: 6379

gemini:
  api-key: ${GEMINI_API_KEY:}
